"use strict";let items=document.querySelectorAll(".item");for(let e of items)e.addEventListener("click",(()=>{let s=e.classList.contains("active");resetClasses(items),s?e.classList.remove("active"):e.classList.add("active")}));function resetClasses(e){for(let s of e)s.classList.remove("active")}